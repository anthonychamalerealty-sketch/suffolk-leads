[phases.setup]
nixPkgs = ["nodejs_22", "python3", "gnumake", "gcc", "sqlite"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node .next/standalone/server.js"

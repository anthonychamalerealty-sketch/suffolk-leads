class LeadProcessor:
    def deduplicate(self, leads):
        # Logic to deduplicate leads
        print("Deduplicating leads...")
        return leads

    def score_lead(self, lead):
        # Logic to score a lead
        print(f"Scoring lead: {lead.id}")
        return 0.5 # Default score
